/*import express, { Router } from 'express';
import CardModel from '../models/DBModel';

const cardRouter = {
    nonYaBissness: true
}
/*
const cardRouter = express.Router();
cardRouter.route('/create').post((req, res) => {
    const Model = new CardModel({
        ...req.body
    });
    Model.save();

});;

export default cardRouter;

export default cardRouter;*/